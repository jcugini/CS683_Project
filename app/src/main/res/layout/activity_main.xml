<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.cuginimotorsports.cs683_project.MainActivity"
    android:background="#FF35393D">

    <!-- This Activity is currently nulled out due to the way the current buttons are configured.
    I originally had it setup so that the welcome screen had a "start" button which would bring you
    to the activity_main screen, but I avoided this by creating two buttons on the welcome_screen
    that point directly at calendar events.-->
    
    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/hintText"
        android:hint="@string/test_hint"
        android:textColorHint="#FF969A9E"
        android:textColor="#ffffff"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <TextView
        style="@style/AppText"
        android:text="@string/Calendar_text"
        android:id="@+id/textView"
        android:layout_above="@+id/hintText"
        android:layout_alignParentStart="true" />

</RelativeLayout>
